{% extends 'base.html.twig' %}

{% block title %}City{% endblock %}

{% block body %}
    <h1>City</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ city.cityId }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ city.name }}</td>
            </tr>
            <tr>
                <th>AirQuality</th>
                <td>{{ city.airQuality }}</td>
            </tr>
            <tr>
                <th>FineParticle</th>
                <td>{{ city.fineParticle }}</td>
            </tr>
            <tr>
                <th>HeavyParticle</th>
                <td>{{ city.heavyParticle }}</td>
            </tr>
            <tr>
                <th>Ozone</th>
                <td>{{ city.ozone }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('city_index') }}">back to list</a>

    <a href="{{ path('city_edit', {'id': city.cityId}) }}">edit</a>

    {{ include('city/_delete_form.html.twig') }}
{% endblock %}
